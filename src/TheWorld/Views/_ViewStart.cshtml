@{
    // This file is merged with every separate page (such as Index.cshtml and etc
    
    // everypage is going to use layout _Layout by default
    // (pages can override this)
    Layout = "_Layout";
}
